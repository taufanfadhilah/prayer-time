# Prayer Time Android TV - Update

## Before

The APK bundled all web assets (HTML/JS/CSS) locally inside the app. Any change (even a small text update) required rebuilding the APK and reinstalling on every TV.

## After

The APK now loads directly from our live site (`prayertime-v2.fonti.dev`). Any change deployed to the web will instantly reflect on all Android TVs — no reinstall needed.

## Action needed

One final APK reinstall on all devices to apply this change. After that, all future updates are automatic.
